services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    # Lets the container reach MySQL running on your Mac via host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"